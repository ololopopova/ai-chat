# 🤖 AI Chat — Умный чат-ассистент

Демонстрационный проект умного чат-приложения с RAG, агентной оркестрацией и инструментами через MCP.

## 🚀 Быстрый старт

### Требования

- Python 3.12+
- pip

### Установка

1. **Клонировать репозиторий:**
```bash
git clone <repository-url>
cd ai-chat
```

2. **Создать виртуальное окружение:**
```bash
python -m venv venv
source venv/bin/activate  # Linux/macOS
# или
venv\Scripts\activate  # Windows
```

3. **Установить зависимости:**
```bash
# Для разработки (включает ruff, mypy, pytest)
pip install -r requirements-dev.txt

# Только для запуска
pip install -r requirements.txt
```

4. **Настроить переменные окружения:**
```bash
cp .env.example .env
# Отредактировать .env при необходимости
```

### Запуск приложения

```bash
streamlit run ui/app.py
```

Приложение будет доступно по адресу: http://localhost:8501

## 📁 Структура проекта

```
ai-chat/
├── config/                 # Конфигурационные файлы
│   └── domains.yaml       # Конфигурация доменов
├── src/core/              # Ядро приложения
│   ├── config.py          # Pydantic Settings
│   ├── exceptions.py      # Базовые исключения
│   └── logging.py         # Настройка логирования
├── ui/                    # Streamlit UI
│   ├── app.py             # Точка входа
│   ├── session.py         # Менеджер сессии
│   ├── api_client.py      # Интерфейс API клиента
│   ├── components/        # UI компоненты
│   └── models/            # Pydantic модели
├── tests/                 # Тесты
└── ...
```

## 🎯 Функциональность (Phase 1)

### Чат-интерфейс
- ✅ История сообщений (user/assistant)
- ✅ Поле ввода с отправкой
- ✅ Аватары для пользователя и ассистента
- ✅ Стриминг ответа (эффект печатания)
- ✅ Отображение изображений в сообщениях
- ✅ Автоскролл к последнему сообщению

### Timeline стадий
- ✅ Отображение текущей стадии обработки
- ✅ История пройденных стадий
- ✅ Визуальное различие статусов (pending/active/completed)
- ✅ Названия стадий на русском языке

### Прогресс операций
- ✅ Прогресс-бар с процентом (0-100%)
- ✅ Описание текущего шага
- ✅ Отображение результата

### Боковая панель
- ✅ Логотип и название
- ✅ Кнопка "Новый диалог"
- ✅ Список диалогов с переключением
- ✅ Переключатель Mock/Real API

## 🧪 Mock сценарии

Для тестирования введите:

| Ключевые слова | Сценарий |
|----------------|----------|
| `баннер`, `картинка` | Генерация баннера с прогрессом |
| `ошибка`, `сломай` | Имитация ошибки |
| `погода`, `анекдот` | Off-topic ответ |
| Любой другой текст | RAG ответ |

## 🛠 Разработка

### Проверка качества кода

```bash
# Линтинг
ruff check .

# Форматирование
ruff format .

# Проверка типов
mypy .
```

### Запуск тестов

```bash
pytest tests/ -v
```

Тесты покрывают: модели данных, timeline, mock-клиент (22 теста).

## 📋 Переменные окружения

| Переменная | Описание | По умолчанию |
|------------|----------|--------------|
| `APP_ENV` | Окружение (development/staging/production) | development |
| `APP_DEBUG` | Режим отладки | false |
| `API_BASE_URL` | URL backend API | http://localhost:8000 |
| `API_WS_URL` | URL WebSocket | ws://localhost:8000 |
| `UI_TITLE` | Заголовок страницы | AI Ассистент |
| `UI_PAGE_ICON` | Иконка страницы | 🤖 |
| `USE_MOCK_API` | Использовать mock вместо реального API | true |

## 📦 Стек технологий

- **Python** 3.12+
- **Streamlit** — UI framework
- **Pydantic** — валидация данных
- **httpx** — HTTP клиент (для будущих фаз)
- **ruff** — линтинг и форматирование
- **mypy** — проверка типов
- **pytest** — тестирование

## 📄 Лицензия

MIT

