# Docker environment variables for AI Chat
# Скопируйте этот файл в docker/.env и настройте значения

# ==========================================
# Infrastructure (secrets & connection strings)
# ==========================================

# PostgreSQL
POSTGRES_USER=ai_chat
POSTGRES_PASSWORD=ai_chat_secret
POSTGRES_DB=ai_chat
POSTGRES_PORT=5433

# Redis
REDIS_PORT=6379

# API Server
API_PORT=8000
APP_ENV=development

# Streamlit UI
UI_PORT=8501

# Adminer (только для отладки)
ADMINER_PORT=8080

# Dozzle - Docker Logs Viewer (только для отладки)
DOZZLE_PORT=9999

# ==========================================
# API Keys (SECRETS - only here!)
# ==========================================
# LLM конфигурация (модели, параметры) загружается из config/llm.yaml
# Здесь только API ключи, которые не должны попадать в репозиторий

# OpenAI API Key (обязательно для работы с OpenAI)
# Получить на https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-your-api-key-here

# Anthropic API Key (опционально, для Claude)
# ANTHROPIC_API_KEY=sk-ant-your-key-here
