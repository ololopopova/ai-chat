# Core dependencies
streamlit>=1.40.0
httpx>=0.27.0
pydantic>=2.9.0
pydantic-settings>=2.5.0

# FastAPI Backend
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
websockets>=13.0
websocket-client>=1.8.0

# Database
sqlalchemy[asyncio]>=2.0.35
asyncpg>=0.30.0
alembic>=1.14.0
pgvector>=0.3.5

# Redis
redis>=5.2.0

# LangGraph + LangChain (December 2025)
langgraph>=0.4.0
langgraph-checkpoint-postgres>=2.0.0
langchain>=0.3.0
langchain-core>=0.3.0
langchain-openai>=0.3.0

# Postgres driver for langgraph checkpointer (psycopg3)
psycopg[binary]>=3.2.0

# Async support for tenacity (retry logic)
tenacity>=9.0.0

# Utils
pyyaml>=6.0.0

# OpenAI SDK (для embeddings и direct API calls)
openai>=1.54.0

# Ingest Service (HTML parsing)
beautifulsoup4>=4.12.0
lxml>=5.0.0

# MCP Protocol
mcp>=1.0.0

# ML for RAG
sentence-transformers>=3.0.0  # Embeddings and cross-encoder reranking
